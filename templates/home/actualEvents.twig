<div class="relative z-30 mb-5 top-24 lg:w-72 block p-5  rounded-xl border border-solid border-black bg-zinc-50 shadow-xl" id="next">
        <p class='text-2xl font-bold text-center'>
            Nächster Termin:<br>{{ firstEvent.eventdate | date_format("d. F Y") }}</p>
        <br>
        <p class='text-xl font-bold text-center'>18 - 22Uhr</p>
        <br>
        <p class='text-lg text-center font-bold'><u>Weitere Termine:</u><br>
        {% for event in events %}
            {{ event.eventdate | date_format("d. F Y") }}<br>
        {% endfor %}
        </p>
</div>